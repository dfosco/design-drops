# Environment Variables
# Copy to .env and fill in values
#
# No env vars are required for basic operation.
# Auth is handled via PAT (pasted by each user in the browser).
# Repo config is in drops.config.json.
