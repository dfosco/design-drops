# Auth Environment Variables
# Copy to .env and fill in values

# GitHub OAuth App client ID (public, safe for client-side)
PUBLIC_GITHUB_CLIENT_ID=

# URL of the token exchange proxy (Cloudflare Worker, serverless function, etc.)
# This proxy receives { code } and returns { access_token } using the client_secret
PUBLIC_OAUTH_PROXY_URL=

# OAuth callback URL (must match GitHub OAuth App settings)
PUBLIC_OAUTH_REDIRECT_URI=http://localhost:4321/design-drops/auth/callback/
