# Auth Environment Variables
# Copy to .env and fill in values

# GitHub App client ID (public, safe for client-side)
# Create a GitHub App at https://github.com/settings/apps — enable OAuth, no secret needed (PKCE)
PUBLIC_GITHUB_CLIENT_ID=

# OAuth callback URL (must match GitHub App settings)
PUBLIC_OAUTH_REDIRECT_URI=http://localhost:4321/design-drops/auth/callback/

# CORS proxy for token exchange (deploy proxy/worker.js to Cloudflare Workers)
# See proxy/README.md for setup instructions
PUBLIC_OAUTH_PROXY_URL=http://localhost:8787

# GitHub Actions Workflow Dispatch
PUBLIC_ACTIONS_TOKEN=
PUBLIC_REPO_OWNER=dfosco
PUBLIC_REPO_NAME=design-drops
